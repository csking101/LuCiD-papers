# Git LFS
*.mp4 filter=lfs diff=lfs merge=lfs -text
*.gif filter=lfs diff=lfs merge=lfs -text

# GitHub Pages: serve MP4s directly (LFS pointers don't work with Pages)
docs/**/*.mp4 !filter !diff !merge
